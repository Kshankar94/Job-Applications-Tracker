Execution steps:

Please open this folder using an IDE (IntelliJ preferably)

Locate the ApplicationTrackerApplication under /src/main/java/com/webGroup/ApplicationTracker/

Run this file as this is annotated with @SpringBootApplication

For the backend database, please install the redis server
for windows. After you download the folder, run ./redis-server

Now, hit the localhost:8082. 